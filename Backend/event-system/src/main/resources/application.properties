# --- SERVER ---
server.port=8080

# --- H2 DATABASE SETUP (In-Memory) ---
spring.datasource.url=jdbc:h2:mem:eventdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# --- HIBERNATE ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- H2 CONSOLE (Optional: To see DB in browser) ---
spring.h2.console.enabled=true

# --- EMAIL (Keep this so emails still work) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:iamjeeva2018@gmail.com}
spring.mail.password=${MAIL_PASSWORD:vdoezogihjqimudo}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true