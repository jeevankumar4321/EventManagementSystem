spring.application.name=event-system
## Database Connection ##
# The URL where your database lives (localhost means your computer)
spring.datasource.url=jdbc:mysql://localhost:3306/event_db

# Your MySQL Username (usually 'root')
spring.datasource.username=root

# Your MySQL Password (CHANGE THIS to your actual password)
spring.datasource.password=jayamschool
## Hibernate Settings ##
# 'update' means: "If I change a Java class, update the MySQL table automatically"
spring.jpa.hibernate.ddl-auto=update

# Show the SQL queries in the console (Good for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

## Email Settings (We will configure this properly later) ##
# Just adding placeholders so the app doesn't crash on startup
# ... (Keep your database settings above) ...

# Email Settings
# ... (Keep your database settings above) ...

# Email Settings
# ... (Keep your database settings above) ...

# Email Settings
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=iamjeeva2018@gmail.com
spring.mail.password=vdoezogihjqimudo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.debug=true